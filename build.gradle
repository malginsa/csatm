group 'learn'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(dir: '/usr/local/algs4', include: ['*.jar'])
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
